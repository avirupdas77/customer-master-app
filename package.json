{
  "name": "customer-master-app",
  "version": "1.0.0",
  "description": "Full Stack Customer Master Module for Inventory Management System",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend start",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
